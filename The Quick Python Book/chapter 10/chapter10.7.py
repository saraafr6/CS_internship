'''Consider a variable width that’s in the module make_window.py. In which of the following contexts is width in
scope?:'''

(A) within the module itself # yes
(B) inside the resize() function in the module # No
(C) within the script that imported the make_window.py module # yes
